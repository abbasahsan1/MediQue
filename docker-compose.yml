services:
  medique-server:
    build:
      context: ./server
    container_name: medique-server
    env_file:
      # Copy server/.env.example to server/.env and fill in real secrets before running
      - ./server/.env
    ports:
      - "4000:4000"
    restart: unless-stopped
